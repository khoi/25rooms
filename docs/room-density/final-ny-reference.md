# New York reference-room final revision

This pass updates only `map-room.js` and `queens-panorama.js` in the main checkout, using their current integrated content. It replaces prominent repeated imagery and stock arrangements rather than adding another accessory layer.

| Room | Visible changes | Review and limitations |
| --- | --- | --- |
| Map room | The two formerly identical wall maps are now a shoreline/block survey and a contour survey with compass and a repaired corner. The tall archive has a cooler recessed frame, upright atlases, offset bound stacks and rolled-map trays with visible ends. The foreground cart has a large unfolding map, turned corner, weights, a repaired atlas spine and a separated lower tray. A raised open reference sits on the flat cabinet. Countertop sheets were moved above their actual supporting surface; the pencil tray was moved onto the main tabletop. Restrained warm light emphasizes the central reference, with a cooler table apron. | At 800px the wall images, upright/rolled/flat storage and cart foldout are distinct. At 1400px page folds, repair stitching and weights are visible. Actor positions and clips remain untouched, and start/hold/return/end were inspected. The central map remains stylized, and the original pencil gesture reads more clearly as holding/lifting than precise paper contact. |
| Queens panorama | Three repeated diagrams became a water-and-block plan, suspension-bridge elevation and comparative building silhouettes. Repeated rear strips became one long topographic section on a dark wall. The generic block blanket was replaced outside the central tower strip by slab blocks with roof fittings, enclosed apartment courts, pitched-roof homes with chimneys, terraces with stoops and a domed civic garden. Existing industrial roofs, rail stock and waterfront fittings remain. Cooler walls/floor frame the brighter model. | At 800px the districts have different massing and roof silhouettes, while all perimeter routes and visitors remain clear. The 1400px view resolves courtyards, chimneys, stoops and civic roof. Pointing poses and wheelchair clearance were checked across start/hold/return/end. The miniature geography remains a fictional compression, not an accurate model of individual neighborhoods. |

Final images were opened with `view_image`, not merely rendered. Evidence is in `/tmp/ny-reference-final/`: both `map-room` and `queens-panorama` have `-normal.png` (800px), `-close.png` (1400px), `-start.png`, `-hold.png`, `-return.png` and `-endpoint.png`. Phase times are 0, 48%, 80% and 100% of each unchanged loop. The normal Queens still remains 8 seconds.

`node scripts/check-rooms.mjs` passed after final edits: 325 unique rooms across 13 collections, valid layout/hit targets, 300 authored rooms in 4144 sampled animation frames, including 250 declared loop boundaries. No commits or pushes were made by this worker.

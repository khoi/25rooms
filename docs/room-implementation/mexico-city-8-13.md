# Mexico City rooms 8–13 implementation review

The six modules use the existing canvas renderer, a 12 by 12 floor, and the shared five-color palette. Each room defines its own fixture layout and timed action. All figure clips have a `mexico` scene prefix. Shared geometry helpers remain unchanged.

The review used the real `RoomPainter` through `/tmp/rooms-200-render/render.mjs`. Every room was inspected at 220, 800, and 1400 pixels. Six sampled poses per room cover preparation, contact, the held pose, return, and the closing pause. The still frames use each module's `stillTime`.

| Room | Developed assemblies | Discoveries visible close up | Motion and contact review | Still pose | Evidence | Remaining limits |
| --- | --- | --- | --- | --- | --- | --- |
| 8. `mexico-city-metal-window` | Freestanding masonry sample arch with bolted feet, tube frame, hinge knuckles, latch, striker, and bracing. Steel stock rack with separate sheet, mesh, tube, handle, and scrap storage. | Rough weld, polished handle strip, padded angle gauge, mesh repair, bolt in a cap, miniature grille, bending jig pins, and mounting bolts. | The leaf rotates about its hinge. The fabricator follows the handle while the customer leans toward the gap. The 22-second return closes the same leaf. Reduced the swing angle after the first render made the grille almost edge-on. | 10.7 seconds. Open grille, visible lattice, and hand beside the latch. | `/tmp/mexico-8-13/metal-window-normal.png`, `metal-window-close.png`, `metal-phases.png`. | The narrow latch tongue needs the close view. The old mounting-hole detail is represented by plate fastening marks rather than a separate drilled hole. |
| 9. `mexico-city-university-model` | Laminated contour landscape on an apron table, removable courtyard building, model stairs, locating pins, tree sockets, and ramp. Side material library with plan drawers, upright samples, models, and tube storage. | Repaired stair, empty tree socket, stone pores, miniature ramp, roof packing nests, shade disk, and laminated cut edges. | The building lifts vertically, pauses, and returns to its original pins over 24 seconds. The classmate points toward the exposed path. The terrain redraw hides the rear student's legs correctly. Rebuilt the material library to expose shelves instead of a blank front. | 11.7 seconds. Building raised above the contour. | `/tmp/mexico-8-13/university-model-normal.png`, `university-model-close.png`, `model-phases.png`. | The far gripping hand is behind the model wall. The short lift reads more clearly at normal and close sizes than at collection size. |
| 10. `mexico-city-book-arcade` | Wheeled cabinet with locked brakes, sloped contents, extended runners, bracketed folio support, and a hinged picture volume. Archive wall with deep book bays, bundles, folios, tube storage, and repair contents. | Fabric spine repair, layered paper edges, linen ties, expandable sleeve folds, paper bird behind books, magnifier, and drawer runners. | The 16-second action extends the drawer, opens the cover about its spine, pauses, closes the cover, then retracts. The seller moves from the pull to the hinge. Moved the seller in front of the live folio layer after the first render hid the head. | 8 seconds. Picture spread open on the supported drawer. | `/tmp/mexico-8-13/book-arcade-normal.png`, `book-arcade-close.png`, `book-phases.png`. | The paper bird and repaired spine require close inspection. The arcade opening uses a shallow arch suited to the room's wall height. |
| 11. `mexico-city-robot-club` | Manual demonstration arm with constant-length links, bearing, shoulder and elbow pins, linkage rod, counterweight, padded gripper, recess, and travel stop. Learning cabinet with wheels, controller, trays, drawers, and closed battery box. | Coral gripper repair, rounded foam block, seed-shaped lever grip, cloth-hinge prototype, four-bar study, gear pair, and revised linkage thickness. | The gripper closes before the arm lifts. The foam block follows the gripper, pauses above its recess, returns, and is released. Link lengths remain fixed throughout the 18-second cycle. The learner's hand follows the lever. | 8.4 seconds. Block held above its recess with the mentor watching the elbow. | `/tmp/mexico-8-13/robot-club-normal.png`, `robot-club-close.png`, `robot-phases.png`. | The linkage and pad repair need the close view. The mentor's nod is deliberately small. |
| 12. `mexico-city-community-pool` | Slatted bench with frame, removable foot platform, pegs, bag, towel, and a separate drainage strip. Raised equipment store with kickboard slots, floats, towels, drawers, bars, and mesh storage. | Repaired locker handle, cap pouch, under-bench float, soft board slots, folded cloth, shoe pair, and float patch. | Both hands lift the goggles from the neck to the forehead, hold, and lower over 20 seconds. The goggle strap stays attached. Corrected the starting height after the first render placed the goggles too near the face. | 19 seconds. Goggles at the neck; friend holds the kickboard. | `/tmp/mexico-8-13/community-pool-normal.png`, `community-pool-close.png`, `pool-phases.png`. | The mirror is a restrained reflection rather than a second rendered figure. The pool remains outside the room's footprint. |
| 13. `mexico-city-seed-drawers` | Seed cabinet with shallow drawers, fitted jar cups, cord pull, folding woven tray, and visible support brackets. Crop dresser with baskets, samples, sieve slots, sacks, envelope, and drying-frame connections. | Uneven cob row, cord pull, saved pod halves, spoon cradle, folded envelope, jar contents, sieve weave, and worn boots. | Two retained jars move separately between the cups and comparison tray. Each follows a lifted path and returns before the tray folds and drawer closes. Added a continuous grower route between transfers. The 22-second loop retains the same jars. | 11 seconds. Open drawer, empty cups, and both jars on the mat. | `/tmp/mexico-8-13/seed-drawers-normal.png`, `seed-drawers-close.png`, `seed-phases.png`. | The cob stays on the fixed ledge directly above the folding tray. The rapid reach back for the second jar is stylized. |

The collection-size sheet is `/tmp/mexico-8-13/collection.png`. The prototype comparison with the original press is `/tmp/mexico-8-13/comparison.png`. The final book and pool comparison with the original library and kitchen is `/tmp/mexico-8-13/library-kitchen-comparison.png`. The original scenes retain denser incidental detail and stronger pools of local light. These six scenes use brighter working interiors and larger clear spaces around their moving objects. The review does not claim that the new scenes exceed the originals.

Direct-import validation adapts `scripts/check-rooms.mjs` in `/tmp/check-mexico-8-13.mjs`. The check validates static geometry, all live drawing coordinates, still times, beat boundaries, and both sides of each declared loop boundary. All six modules pass. Render review also checked fixture support, interior wall offsets, canvas bounds, hand visibility, and the final return poses.

The worker changed only these six modules and this review file. Collection integration, browser navigation checks, staging, commit, and push belong to the coordinating agent.
